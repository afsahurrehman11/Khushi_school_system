# Heroku-optimized requirements (CPU-only, minimal footprint)
# This file removes heavy GPU dependencies for Heroku deployment

# Core Framework
pydantic==1.10.12
fastapi==0.103.2
uvicorn==0.23.2
starlette==0.27.0

# Database
pymongo==4.15.4

# Authentication
python-jose==3.3.0
passlib==1.7.4

# File handling
python-multipart==0.0.6
python-dotenv==1.0.1
openpyxl==3.1.2

# Core utilities
typing-extensions==4.12.0
Pillow==10.0.1
numpy>=1.26.4
requests==2.31.0
httpx>=0.24.0

# PDF/Reports
reportlab>=4.0.0
matplotlib>=3.8.0

# System monitoring
psutil>=5.9.0

# Image processing (lightweight, no ML)
opencv-python-headless==4.8.0.76

# ============================================
# FACE RECOGNITION - CPU-ONLY MINIMAL BUILD
# ============================================
# Using PyTorch CPU-only build (saves ~500MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu
facenet-pytorch>=2.2.0

# ============================================
# REMOVED FOR HEROKU (too heavy):
# - deepface (pulls tensorflow ~700MB)
# - tensorflow (not needed if using PyTorch FaceNet)
# - tf-keras (tensorflow dependency)
# ============================================
