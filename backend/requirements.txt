# ============================================
# CMS Backend - Heroku Optimized (CPU-Only)
# ============================================
# Last updated: 2026-03-05
# Using: ONNX Runtime + ArcFace ResNet100 for accurate face recognition
# Total ML footprint: ~186MB (vs ~400MB with PyTorch)

# --- Core Framework ---
pydantic==1.10.12
fastapi==0.103.2
uvicorn==0.23.2
starlette==0.27.0

# --- Database ---
pymongo==4.15.4

# --- Authentication ---
python-jose==3.3.0
passlib==1.7.4

# --- File Handling ---
python-multipart==0.0.6
python-dotenv==1.0.1
openpyxl==3.1.2

# --- Core Utilities ---
typing-extensions==4.12.0
Pillow==10.0.1
numpy>=1.26.4
requests==2.31.0
httpx>=0.24.0

# --- System Monitoring ---
psutil>=5.9.0

# --- Image Processing (headless = smaller, no GUI deps) ---
opencv-python-headless==4.8.0.76

# ============================================
# FACE RECOGNITION - ARCFACE RESNET100 (ACCURATE)
# ============================================
# ONNX Runtime CPU (~15MB) - Much lighter than PyTorch (~280MB)
onnxruntime==1.17.0

# ============================================
# MODEL: ArcFace ResNet100 (downloaded at runtime)
# - Size: ~170MB (high accuracy)
# - Accuracy: 99.77% on LFW (better than FaceNet)
# - 512-dimensional embeddings with L2 normalization
# - Uses <0.5GB RAM for real-time recognition
# ============================================

# ============================================
# REMOVED (to reduce slug size):
# - torch (~280MB) - replaced with onnxruntime (~15MB)
# - torchvision (~50MB) - not needed
# - facenet-pytorch (~107MB model) - replaced with MobileFaceNet (~4MB)
# - tensorflow, deepface, tf-keras - not needed
# ============================================
